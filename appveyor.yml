version: 1.5.{build}

branches:
  only:
  - master

environment:
  VK_VERSION: 1.2.162.1
  VULKAN_SDK: c:\VulkanSDK\%VK_VERSION%

skip_tags: true
image: Visual Studio 2019
clone_depth: 1
test: off

configuration:
- Debug
- Release

platform:
- x64
- x86

install:
- ps: curl -v -o VulkanSDK.exe https://sdk.lunarg.com/sdk/download/$env:VK_VERSION/windows/vulkan_sdk.exe?Human=true

before_build:
- cmd: .\VulkanSDK.exe /S

build:
  project: quake2.sln
  parallel: true
  verbosity: normal

notifications:
- provider: Email
  to:
  - krzysztof.kondrak@gmail.com
  subject: vkQuake2 {{buildVersion}} - {{status}}
  on_build_success: false
  on_build_failure: true
  on_build_status_changed: true
